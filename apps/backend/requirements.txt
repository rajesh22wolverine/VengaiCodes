# ═══════════════════════════════════════════════════════════════
#  VengaiCode — Backend Production Dependencies
#  requirements.txt
#
#  Install: pip install -r requirements.txt --break-system-packages
#  (or inside a venv: pip install -r requirements.txt)
# ═══════════════════════════════════════════════════════════════

# ── Core Framework ──
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0

# ── Database ──
sqlalchemy[asyncio]==2.0.27
asyncpg==0.29.0
aiosqlite==0.19.0
alembic==1.13.1

# ── Security (used by core/security.py) ──
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==42.0.2

# ── Redis ──
redis[hiredis]==5.0.1

# ── HTTP Client (Ollama, Groq, external APIs) ──
httpx==0.26.0

# ── AI / LangChain ──
langchain==0.1.7
langchain-community==0.0.20

# ── Payments ──
razorpay==1.4.1

# ── OTP ──
pyotp==2.9.0

# ── Document Generation ──
weasyprint==60.2
python-docx==1.1.0
docxtpl==0.16.7
jinja2==3.1.3

# ── Error Tracking ──
sentry-sdk[fastapi]==1.40.0

# ── Utilities ──
python-multipart==0.0.7
email-validator==2.1.0.post1
python-dotenv==1.0.1