# ═══════════════════════════════════════════════════════════════
#  VengaiCode — Backend Production Dependencies
#  requirements.txt
#
#  Install: pip install -r requirements.txt --break-system-packages
#  (or inside a venv: pip install -r requirements.txt)
# ═══════════════════════════════════════════════════════════════

# ── Core Framework ──
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0

# ── Database ──
sqlalchemy[asyncio]==2.0.27
asyncpg==0.29.0
aiosqlite==0.19.0
alembic==1.13.1

# ── Security (used by core/security.py) ──
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt backend self-test (detect_wrap_bug) hashes a
# fixed >72-byte secret expecting silent truncation (old bcrypt
# behavior). bcrypt>=4.0 raises ValueError instead, which crashes
# EVERY password hash/verify (signup, login) the first time passlib
# lazy-loads the backend. Pinned below 4.0 — passlib 1.7.4 was never
# updated for bcrypt's breaking change.
bcrypt==3.2.2
cryptography==42.0.2

# ── QR codes (Share via QR — services/qr_share.py) ──
# Pure Python, no dependencies (writes PNG itself, no Pillow), BSD licence.
segno==1.6.6

# ── Grammar-based syntax checking of generated code (ai/syntax_check.py) ──
# One small prebuilt grammar per language (~4.5 MB in total on Linux),
# all MIT — the individual packages, NOT tree-sitter-language-pack 1.x,
# which downloads grammars from the internet at run time.
tree-sitter==0.25.2
tree-sitter-python==0.25.0
tree-sitter-javascript==0.25.0
tree-sitter-typescript==0.23.2
tree-sitter-java==0.23.5
tree-sitter-kotlin==1.1.0
tree-sitter-c-sharp==0.23.5
tree-sitter-go==0.25.0
tree-sitter-rust==0.24.2
tree-sitter-php==0.24.1
tree-sitter-ruby==0.23.1
tree-sitter-dart==0.1.0
tree-sitter-swift==0.7.3
tree-sitter-gdscript==6.1.0
tree-sitter-lua==0.5.0
tree-sitter-cpp==0.23.4
tree-sitter-html==0.23.2
tree-sitter-css==0.25.0
tree-sitter-json==0.24.8
tree-sitter-yaml==0.7.2
tree-sitter-toml==0.7.0
tree-sitter-xml==0.7.0
tree-sitter-bash==0.25.1

# ── Redis ──
redis[hiredis]==5.0.1

# ── HTTP Client (Ollama, Groq, external APIs) ──
httpx==0.26.0

# ── AI / LangChain ──
langchain==0.1.7
langchain-community==0.0.20

# ── Payments ──
razorpay==1.4.1

# ── OTP ──
pyotp==2.9.0

# ── Document Generation ──
weasyprint==60.2
python-docx==1.1.0
docxtpl==0.16.7
jinja2==3.1.3

# ── Error Tracking ──
sentry-sdk[fastapi]==1.40.0

# ── Utilities ──
python-multipart==0.0.7
email-validator==2.1.0.post1
python-dotenv==1.0.1